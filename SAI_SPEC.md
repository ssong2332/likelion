# 🚀 글로벌 업무 메시지 AI 교정 도구 '사이 (Sai)' - 통합 명세서

본 문서는 글로벌 협업 소통 도우미 **'사이 (Sai)'**의 핵심 비즈니스 방향성, 요구사항 및 기능 상세를 하나로 통합한 마스터 명세서입니다.

---

## 1. 📌 제품 개요
* **제품명**: 사이 (Sai) - 사람과 사람, 언어와 언어 사이를 매끄럽게 잇는 소통 도우미
* **타겟 언어**: 한국어 ↔ 영어, 중국어 (v1 핵심 타겟)
* **디자인 컨셉**: 무채색(White/Gray) Base + 주황(Orange)/초록(Green) Accent

---

## 2. 🚫 핵심 구현 금지사항 (Guiding Principles)
> 이 규칙들은 다른 모든 기능 요구사항보다 최우선하여 적용됩니다.

* **G1/G2 (개인 서열화 및 우열 낙인 금지)**: 개인에게 점수, 등급, 순위, 백분위 등을 부여하여 타인에게 노출하거나 좋음/나쁨의 우열 축으로 표현하는 것을 전면 금지합니다.
* **G3/G4 (Zero Retention - 본문 저장 금지)**: 사용자의 메시지 본문을 당사 서버, DB, 로그에 절대 저장하지 않습니다. 원본 텍스트는 브라우저 메모리 내에서 처리 후 즉시 파기합니다. (OpenAI API의 벤더사 일시 보관 정책은 정직하게 안내하고, 로컬 차단 가드로 보완합니다.)
* **G5 (실패 인지 보장)**: 교정이나 텍스트 치환, 클립보드 복사 등이 실패했을 때 조용히 묻히지 않고, 사용자에게 경고와 함께 수동 복사 등의 확실한 대안을 안내해야 합니다.
* **G6 (API 호출 최소화)**: 한 번의 API 호출로 톤 교정, 번역, 역번역, 체크리스트 판정 등의 결과를 구조화된 JSON으로 일괄 수신해야 합니다.
* **G7 (Work-Safe 필터)**: 비속어, 혐오 표현, 정치적 함의가 섞인 인터넷 밈의 무분별한 유입을 사전에 차단합니다.

---

## 3. 🖥️ UI 구성 및 3원화 아키텍처

### 3.1. In-page floating 팝업
사용자가 웹 페이지(Slack, Gmail 등)에서 텍스트를 드래그 선택할 때 띄워지는 소형 대화식 UI.
* **작성 모드 (Compose Mode)**: 입력창 내부의 텍스트 선택 시 동작. 톤 교정, 번역, 역번역, 체크리스트, 티켓 변환 등의 모든 편집 제어판 제공.
* **수신 모드 (Decode Mode)**: 읽기 전용 텍스트 선택 시 동작. 직역, 상대의 실제 의도, 요구 행동 및 긴급도 해석 결과만 제공.

### 3.2. 크롬 사이드 패널 (Side Panel)
온보딩, 용어사전 관리, 개인 프로필 성향 설정, 저장된 스니펫, 포인트 확인용 종합 설정 패널.
* **용어 사전 탭**: 개인/팀/연동 용어의 추가/삭제 및 원문유지 설정.
* **스니펫 탭**: 자주 쓰는 refined 문장 보관함.
* **프로필·학습 탭**: 선호 톤 지정, AI가 학습한 문장 패턴 수정/삭제, API Key 입력창.
* **팀 대시보드 (Mock)**: B2B 버전의 조직 건강도 시각화 목업 안내 배너 표시.

### 3.3. Web Dashboard (v1 축소판)
프로젝트 내에서 승인되고 기록된 결정사항(Decision Log)을 카테고리별 목록으로 조회하고, AI 3줄 요약 브리핑을 확인할 수 있는 웹 공유 페이지.

---

## 4. 🔴 MVP 핵심 기능 요구사항 (v1)

### F-01. 긴급도 판정 및 톤 변환
* 사용자가 선택하지 않아도 AI가 문맥을 분석하여 1차 긴급도(`low`, `normal`, `critical`)를 판정해 기본값으로 세팅하고, 판정 이유를 툴팁으로 제공합니다.
* 사용자가 긴급도를 변경하면 변경된 값으로 즉시 재교정합니다.
* 톤 순화로 인해 본래의 긴급한 뉘앙스가 소실된 경우, 자동으로 긴급성을 복원하는 2차 재생성 알고리즘을 적용합니다 (1회 제한).

### F-02. 번역 및 역번역 노출
* 영어/중국어로 번역된 결과물 바로 하단에 **역번역(다시 한국어로 번역한 뜻)을 항상 기본 노출**하여 번역 품질을 스스로 검증하도록 돕습니다.
* 역번역 영역은 토글(ON/OFF)할 수 있으며 상태는 브라우저 로컬 저장소에 보존됩니다.

### F-03. 하소연 → 태스크 티켓 변환
* 감정적인 불만이나 하소연 텍스트가 감지(`detectedIntent === "complaint"`)되면 "티켓 형태로 변환할까요?" 팝업을 띄웁니다.
* 수락 시 문장을 **[문제점 / 영향 / 요청사항]**의 3단 구조화된 티켓 텍스트로 재구성하고 감정 수치는 `concernLevel` 메타데이터로 기록합니다.

### F-04. 심리스 교체 및 5초 Undo
* 교정 완료된 텍스트를 버튼 클릭 시 원래의 입력창에 직접 치환(Replace)합니다.
  - 1단계: `InputEvent` 디스패치 삽입 (React 컴포넌트 등)
  - 2단계: `value` / `textContent` 직접 대입 및 이벤트 트리거
  - 3단계: 실패 시 클립보드에 자동 복사 처리 및 토스트 안내
* 텍스트 치환 완료 후 **5초간 [실행 취소] 토스트**를 띄워 원문 복구를 지원합니다.

### F-05. 퇴근 요정 (비동기 예약 발송)
* 상대방의 현지 시각 기준으로 퇴근 시간(18:00 ~ 09:00) 또는 주말/공휴일인 경우를 자동 감지합니다.
  - `low` 긴급도: 즉시 발송을 강제 제한하고 다음 영업일 아침 9시 예약 발송만 제공.
  - `normal` 긴급도: 예약 발송을 제안하되 거절 시 즉시 발송.
  - `critical` 긴급도: 시차 제한 없이 즉시 발송.

### F-06. 용어 사전 (Glossary)
* 크롬 사이드 패널에서 개인/팀/연동 용어를 CRUD 관리합니다.
* 우선순위: `개인` > `팀` > `기본 AI`.
* `[원문 유지]` 설정 시 번역하지 않고 스펠링을 그대로 보존합니다.
* 교정 결과창에서 용어 사전이 적용된 단어는 **🟢 초록색 하이라이트** 및 호버 툴팁을 제공합니다.

### F-07. 수신자 소통 가이드
* 점수 대신 서술형 성향 태그(`오전 응답 빠름`, `직접적 표현 선호` 등)로 상대의 소통 방식을 안내합니다.
* 대상자 본인의 통제권 보장을 위해 본인의 가이드 열람, 수정, 비공개 권리를 제공해야 합니다.

### F-08. 수신자 프로필 참고 우선순위
* 국가 단위의 고정관념 편향 방지를 위해 아래 가중치 순서로 분석합니다.
  - 1순위: 사용자가 명시적으로 입력한 성향 및 상황 템플릿
  - 2순위: 3회 이상 축적된 개인 수정 패턴 (Diff 학습)
  - 3순위 (가장 낮음): 해당 국가/문화권 일반 특성 (1, 2순위 부재 시에만 참고)

### F-09. 수동 큐레이션 기반 밈 적용 및 해설 (v1)
* 안전하고 비즈니스에 사용 가능한 캐주얼 밈 DB(미국/중국 각 15~25개)를 구축하여 친근한 톤 적용 시 활용합니다.
* 교정문에 삽입된 밈은 점선 밑줄이 표시되며 호버 시 밈의 의미와 뉘앙스 해설 팝업이 노출됩니다.

### F-11. 수신 메시지 해독기 (Incoming Message Decoder)
* 상대방의 메시지를 드래그하면 직역 번역뿐 아니라 **상대의 실제 의도, 체감 긴급도, 요구 행동, 완곡 표현 경고**를 종합해서 출력합니다.
  - 예: "I have a few minor comments" ➔ 실제 의도: "전면 재작업 요구"

### F-12. 스레드 맥락 반영
* 슬랙 등 스레드 구조에서 직전 대화 5개(최대 2,000자)를 긁어와 대화의 맥락에 맞게 교정 품질을 개선합니다.
* 맥락 반영 여부를 화면에 투명하게 표시하고, 끌 수 있는 옵션을 제공합니다.

### F-13. 채널 유형 인식 및 DM 전환 유도
* 1:1 DM과 공개 채널을 감지하여 톤을 조절합니다. (감지 실패 시 보수적으로 공개 채널로 판정)
* 공개 채널에서 하소연/불만(`complaint`) 감지 시 "공개 채널입니다. DM으로 보낼까요?" 팝업을 안내합니다.

### F-14. 로컬 민감정보 발송 가드
* LLM API 전송 전, 클라이언트 단에서 정규식(Regex)을 사용해 API Key, 주민등록번호, 카드 번호, 비밀번호 유형을 자동 감지합니다.
* 감지 시 AI 전송을 원천 차단하고 `[REDACTED]` 마스킹 후 진행할지, 수동 제외할지 선택권을 줍니다.

### F-15. 온보딩
* 최초 사이드 패널 오픈 시 사용 언어, 협업 대상 국가, 선호 톤 설정을 유도하는 3문항 설문 패널을 제공합니다.

### F-16. 승인 문장 스니펫
* 사용자가 마음에 드는 교정문을 원클릭으로 스니펫 저장소에 등록하여 사이드 패널에서 언제든 재사용할 수 있도록 돕습니다.

### F-17. 학습 내역 열람 및 개별 삭제
* AI가 학습한 내 수정 패턴 목록을 사람이 읽을 수 있는 문장 형태로 프로필 탭에 투명하게 노출하고 개별/전체 삭제 권한을 부여합니다.

### F-18. 위험 표현 하이라이트
* 명령조(`You must`), 과도한 사과(`sorry` 3회 이상), 오해를 부르는 문화적 이모지(`🙏`) 등을 감지하여 **🟡 노란색 밑줄** 경고 및 안전한 대체 이모지로 자동 교환합니다.

### F-19. 3초 문화 진단 체크리스트
* 문장 전체의 구조를 분석하여 영미권 피드백 샌드위치 적용 여부, 마감일 명확성, 요청 구체성 등을 체크해 3개의 아이템 진단 결과(`✓`, `⚠️`, `✕`)를 보여줍니다.

### F-20. 통화·단위 자동 병기
* 수신 국가 단위를 기준으로 금액(원화 ➔ 달러/위안화), 날짜(숫자 ➔ 월 이름 영어 표기), 무게/거리/온도를 환산하여 괄호 병기합니다.

### F-21. 상대국 대형 연휴 사전 알림
* 중국 춘절, 미국 추수감사절 등 3일 이상의 대형 연휴 시작 3일 전부터 팝업 상단에 알림 배너를 띄우고 예약을 권장합니다.

### F-23. 포인트(재화) 시스템
* 협업 친화적 행동(퇴근 요정 예약 수락, 피드백 참여, 온보딩 완료 등)을 할 때 포인트를 지급하고, 상대방의 소통 가이드 상세 열람이나 용어집 확장 시 차감하는 포인트 관리 시스템을 구축합니다.

### F-27. 결정사항 자동 요약 (Decision Log)
* 대화 내용 중 결정사항(일정/기능/비용)을 자동 추출하여 유저 확정을 거친 후 파생 결정 문서 데이터베이스에 기록하고, 마크다운 복사 및 즉시 공유 기능을 제공합니다.

### F-25. 프로젝트 대시보드 (목록형)
* F-27에서 수집한 팀의 중요 의사 결정사항들을 카테고리별 목록과 AI 3줄 요약 브리핑으로 웹에 공유하고, 항목 클릭 시 결론 티켓 내용을 조회할 수 있도록 제공합니다.

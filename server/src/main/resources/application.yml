server:
  port: 8080

spring:
  application:
    name: manyfast-server

openai:
  api-key: ${OPENAI_API_KEY:}
  model: gpt-4o-mini
  api-url: https://api.openai.com/v1/chat/completions

cors:
  allowed-origins:
    - "http://localhost:5173"
    - "http://localhost:3000"
